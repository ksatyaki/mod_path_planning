session: chitt-testing
init_cmd: |
  cd @TMULE_CONFIG_DIR@ || true
  source ./setup.sh

windows:
  - name: roscore
    panes:
      - roscore
    check: "rostopic list"

  - name: stef-map
    panes:
      - roslaunch mod_path_planning stef_atc.launch --screen

  - name: planner
    panes:
      - sleep 60; roslaunch mod_path_planning planner_atc.launch --screen

  - name: scripts
    panes:
      - sleep 120; rosrun mod_path_planning simple_test.sh
